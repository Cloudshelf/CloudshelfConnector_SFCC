mutation DeleteProductGroups($ids: [GlobalId!]!) {
    deleteProductGroups(ids: $ids) {
        productGroups {
            id
        }
        userErrors {
            code
            message
        }
    }
}
