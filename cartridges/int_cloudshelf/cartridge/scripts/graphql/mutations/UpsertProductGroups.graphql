mutation UpsertProductGroups($input: [ProductGroupInput!]!) {
    upsertProductGroups(input: $input) {
        productGroups {
            id
        }
        userErrors {
            code
            message
        }
    }
}
