mutation UpsertProducts($input: [ProductInput!]!) {
    upsertProducts(input: $input) {
        products {
            id
        }
        userErrors {
            code
            message
        }
    }
}